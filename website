<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="Advicore Fin Technologies - Next Generation Core Banking Platform for Cooperative Banks, MFIs and Financial Organizations">
<meta name="keywords" content="Core Banking System, CBS, Microfinance, Cooperative Bank, MFI, Banking Software">
<title>Advicore Fin Technologies | Core Banking System</title>

<link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<link href="https://unpkg.com/lucide@latest/dist/umd/lucide.js" rel="stylesheet">

<style>
  :root {
    --navy: #0a1628;
    --navy-mid: #112240;
    --navy-light: #1e3a5f;
    --gold: #d4a843;
    --gold-light: #f0c96b;
    --teal: #00b8a9;
    --white: #f8fafc;
    --text-muted: #94a3b8;
    --card-bg: #142038;
    --border: rgba(212,168,67,0.15);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--navy);
    color: var(--white);
    font-family: 'DM Sans', sans-serif;
    overflow-x: hidden;
  }

  /* ── NOISE TEXTURE OVERLAY ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 0;
    opacity: 0.4;
  }

  /* ── HEADER ── */
  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 18px 80px;
    background: rgba(10, 22, 40, 0.95);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 1000;
    transition: padding 0.3s;
  }

  .logo-area {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .logo-icon {
    width: 44px;
    height: 44px;
    background: linear-gradient(135deg, var(--gold), var(--teal));
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Sora', sans-serif;
    font-weight: 800;
    font-size: 18px;
    color: var(--navy);
  }

  .logo-text {
    font-family: 'Sora', sans-serif;
    font-weight: 700;
    font-size: 18px;
    color: var(--white);
    line-height: 1.2;
  }

  .logo-text span {
    display: block;
    font-size: 11px;
    font-weight: 400;
    color: var(--gold);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  nav { display: flex; align-items: center; gap: 36px; }

  nav a {
    text-decoration: none;
    color: var(--text-muted);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.03em;
    transition: color 0.2s;
    position: relative;
  }

  nav a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--gold);
    transition: width 0.3s;
  }

  nav a:hover { color: var(--white); }
  nav a:hover::after { width: 100%; }

  .nav-cta {
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    color: var(--navy) !important;
    padding: 10px 22px;
    border-radius: 6px;
    font-weight: 600 !important;
    transition: opacity 0.2s, transform 0.2s !important;
  }

  .nav-cta:hover { opacity: 0.9; transform: translateY(-1px); }
  .nav-cta::after { display: none; }

  /* hamburger */
  .hamburger { display: none; cursor: pointer; flex-direction: column; gap: 5px; }
  .hamburger span { width: 24px; height: 2px; background: var(--white); border-radius: 2px; transition: 0.3s; }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 120px 40px 80px;
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 80% 60% at 50% 0%, rgba(0,184,169,0.12) 0%, transparent 60%),
                radial-gradient(ellipse 60% 50% at 80% 80%, rgba(212,168,67,0.08) 0%, transparent 50%),
                var(--navy);
  }

  /* animated grid lines */
  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(212,168,67,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(212,168,67,0.04) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 30%, transparent 100%);
  }

  .hero-content { position: relative; z-index: 1; max-width: 860px; }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(212,168,67,0.1);
    border: 1px solid rgba(212,168,67,0.3);
    color: var(--gold);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 6px 16px;
    border-radius: 100px;
    margin-bottom: 28px;
    animation: fadeUp 0.6s ease both;
  }

  .hero-badge::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--gold);
    border-radius: 50%;
    animation: pulse 1.5s infinite;
  }

  @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.3; } }

  .hero h1 {
    font-family: 'Sora', sans-serif;
    font-size: clamp(38px, 6vw, 72px);
    font-weight: 800;
    line-height: 1.08;
    letter-spacing: -0.02em;
    margin-bottom: 20px;
    animation: fadeUp 0.6s ease 0.1s both;
  }

  .hero h1 .highlight {
    background: linear-gradient(90deg, var(--gold), var(--teal));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero p {
    font-size: 18px;
    color: var(--text-muted);
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: 1.7;
    animation: fadeUp 0.6s ease 0.2s both;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeUp 0.6s ease 0.3s both;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    color: var(--navy);
    border: none;
    padding: 14px 32px;
    border-radius: 8px;
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 0 30px rgba(212,168,67,0.25);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 50px rgba(212,168,67,0.4);
  }

  .btn-outline {
    background: transparent;
    color: var(--white);
    border: 1px solid rgba(255,255,255,0.2);
    padding: 14px 32px;
    border-radius: 8px;
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    transition: border-color 0.2s, background 0.2s;
  }

  .btn-outline:hover {
    border-color: var(--gold);
    background: rgba(212,168,67,0.07);
  }

  /* floating cards */
  .hero-float {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
    margin-top: 70px;
    animation: fadeUp 0.6s ease 0.4s both;
  }

  .float-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 16px 24px;
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 14px;
  }

  .float-card .icon {
    width: 36px; height: 36px;
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    background: rgba(0,184,169,0.12);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── STATS ── */
  .stats-band {
    background: var(--card-bg);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 50px 80px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 40px;
    text-align: center;
  }

  .stat-item .num {
    font-family: 'Sora', sans-serif;
    font-size: 42px;
    font-weight: 800;
    background: linear-gradient(135deg, var(--gold), var(--teal));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .stat-item p { color: var(--text-muted); font-size: 13px; margin-top: 4px; }

  /* ── SECTIONS ── */
  section { position: relative; z-index: 1; }

  .section { padding: 100px 80px; }

  .section-label {
    display: inline-block;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 12px;
  }

  .section-title {
    font-family: 'Sora', sans-serif;
    font-size: clamp(28px, 4vw, 44px);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin-bottom: 16px;
  }

  .section-subtitle {
    color: var(--text-muted);
    font-size: 16px;
    line-height: 1.7;
    max-width: 540px;
    margin-bottom: 60px;
  }

  /* ── FEATURES ── */
  #features { background: var(--navy-mid); }

  .features-header { text-align: center; }
  .features-header .section-subtitle { margin: 0 auto 60px; }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
  }

  .feature-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 32px;
    position: relative;
    overflow: hidden;
    transition: border-color 0.3s, transform 0.3s;
    cursor: default;
  }

  .feature-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 2px;
    background: linear-gradient(90deg, var(--gold), var(--teal));
    opacity: 0;
    transition: opacity 0.3s;
  }

  .feature-card:hover {
    border-color: rgba(212,168,67,0.35);
    transform: translateY(-4px);
  }

  .feature-card:hover::before { opacity: 1; }

  .feature-emoji {
    font-size: 28px;
    margin-bottom: 16px;
    display: block;
  }

  .feature-card h3 {
    font-family: 'Sora', sans-serif;
    font-size: 17px;
    font-weight: 600;
    margin-bottom: 10px;
  }

  .feature-card p { color: var(--text-muted); font-size: 14px; line-height: 1.7; }

  /* ── MODULES SECTION ── */
  .modules-wrap {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .modules-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-top: 24px;
  }

  .module-item {
    display: flex;
    align-items: center;
    gap: 14px;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 16px 20px;
    transition: border-color 0.2s;
  }

  .module-item:hover { border-color: rgba(0,184,169,0.4); }

  .module-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--teal);
    flex-shrink: 0;
  }

  .module-item span { font-size: 14px; font-weight: 500; }

  .modules-visual {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 36px;
    text-align: center;
  }

  .module-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
    margin-top: 20px;
  }

  .tag {
    background: rgba(0,184,169,0.1);
    border: 1px solid rgba(0,184,169,0.25);
    color: var(--teal);
    font-size: 12px;
    font-weight: 500;
    padding: 6px 14px;
    border-radius: 100px;
  }

  /* ── DEMO CTA ── */
  .demo-section {
    background: linear-gradient(135deg, var(--navy-light), var(--navy-mid));
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 100px 80px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .demo-section::before {
    content: '';
    position: absolute;
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(212,168,67,0.07) 0%, transparent 70%);
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }

  .demo-section h2 {
    font-family: 'Sora', sans-serif;
    font-size: clamp(28px, 4vw, 48px);
    font-weight: 700;
    margin-bottom: 16px;
  }

  .demo-section p { color: var(--text-muted); font-size: 16px; margin-bottom: 36px; }

  /* ── PRICING ── */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
  }

  .price-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 36px;
    transition: transform 0.3s, border-color 0.3s;
    position: relative;
  }

  .price-card.featured {
    border-color: var(--gold);
    background: linear-gradient(160deg, rgba(212,168,67,0.08) 0%, var(--card-bg) 60%);
  }

  .price-card:hover { transform: translateY(-6px); }

  .price-badge {
    position: absolute;
    top: -12px; left: 50%; transform: translateX(-50%);
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    color: var(--navy);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 4px 16px;
    border-radius: 100px;
  }

  .price-card h3 {
    font-family: 'Sora', sans-serif;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .price-card .price-amount {
    font-family: 'Sora', sans-serif;
    font-size: 36px;
    font-weight: 800;
    color: var(--gold);
    margin: 16px 0 8px;
  }

  .price-card .price-sub {
    color: var(--text-muted);
    font-size: 13px;
    margin-bottom: 24px;
  }

  .price-features { list-style: none; }

  .price-features li {
    color: var(--text-muted);
    font-size: 14px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255,255,255,0.04);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .price-features li::before {
    content: '✓';
    color: var(--teal);
    font-weight: 700;
    flex-shrink: 0;
  }

  .price-btn {
    width: 100%;
    margin-top: 28px;
    padding: 12px;
    border-radius: 8px;
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: 0.2s;
  }

  .price-btn-secondary {
    background: transparent;
    border: 1px solid rgba(255,255,255,0.15);
    color: var(--white);
  }

  .price-btn-secondary:hover {
    border-color: var(--gold);
    color: var(--gold);
  }

  .price-btn-primary {
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    border: none;
    color: var(--navy);
  }

  /* ── TESTIMONIALS ── */
  #testimonials { background: var(--navy-mid); }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px;
  }

  .testi-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 32px;
  }

  .stars { color: var(--gold); font-size: 14px; margin-bottom: 16px; }

  .testi-card blockquote {
    font-size: 14px;
    color: #cbd5e1;
    line-height: 1.8;
    margin-bottom: 20px;
    font-style: italic;
  }

  .testi-author { display: flex; align-items: center; gap: 12px; }

  .testi-avatar {
    width: 40px; height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--gold), var(--teal));
    display: flex; align-items: center; justify-content: center;
    font-family: 'Sora', sans-serif;
    font-weight: 700;
    font-size: 14px;
    color: var(--navy);
  }

  .testi-name { font-weight: 600; font-size: 14px; }
  .testi-role { color: var(--text-muted); font-size: 12px; }

  /* ── CONTACT ── */
  #contact { background: var(--navy); }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 60px;
    align-items: start;
  }

  .contact-info h2 {
    font-family: 'Sora', sans-serif;
    font-size: 36px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .contact-info p {
    color: var(--text-muted);
    font-size: 15px;
    line-height: 1.8;
    margin-bottom: 36px;
  }

  .contact-detail {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-bottom: 16px;
  }

  .contact-detail .icon {
    width: 42px; height: 42px;
    border-radius: 10px;
    background: rgba(212,168,67,0.1);
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
  }

  .contact-detail p { color: var(--text-muted); font-size: 14px; margin: 0; }
  .contact-detail strong { color: var(--white); display: block; font-size: 15px; }

  .contact-form {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 40px;
  }

  .contact-form h3 {
    font-family: 'Sora', sans-serif;
    font-size: 22px;
    font-weight: 600;
    margin-bottom: 28px;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    margin-bottom: 16px;
  }

  .form-group label {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    margin-bottom: 6px;
    text-transform: uppercase;
  }

  .form-group input,
  .form-group textarea,
  .form-group select {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.1);
    color: var(--white);
    padding: 12px 16px;
    border-radius: 8px;
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group textarea:focus,
  .form-group select:focus {
    border-color: var(--gold);
  }

  .form-group select option { background: var(--navy-mid); }
  .form-group textarea { resize: none; }

  .form-submit {
    width: 100%;
    padding: 14px;
    background: linear-gradient(135deg, var(--gold), var(--gold-light));
    color: var(--navy);
    border: none;
    border-radius: 8px;
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    font-weight: 700;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
    margin-top: 8px;
  }

  .form-submit:hover { opacity: 0.9; transform: translateY(-1px); }

  /* ── FOOTER ── */
  footer {
    background: #060e1a;
    border-top: 1px solid var(--border);
    padding: 60px 80px 30px;
  }

  .footer-top {
    display: grid;
    grid-template-columns: 1.5fr repeat(3, 1fr);
    gap: 40px;
    margin-bottom: 50px;
  }

  .footer-brand .logo-text { font-size: 20px; margin-bottom: 12px; }

  .footer-brand p {
    color: var(--text-muted);
    font-size: 13px;
    line-height: 1.8;
    margin-bottom: 20px;
    max-width: 280px;
  }
  


  .footer-col h4 {
    font-family: 'Sora', sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 16px;
  }

  .footer-col a {
    display: block;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 13px;
    margin-bottom: 10px;
    transition: color 0.2s;
  }

  .footer-col a:hover { color: var(--white); }

  .footer-bottom {
    border-top: 1px solid var(--border);
    padding-top: 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }

  .footer-bottom p { color: var(--text-muted); font-size: 13px; }

  .footer-links { display: flex; gap: 24px; }

  .footer-links a {
    color: var(--text-muted);
    text-decoration: none;
    font-size: 13px;
    transition: color 0.2s;
  }

  .footer-links a:hover { color: var(--gold); }

  /* ── WHATSAPP ── */
  .whatsapp {
    position: fixed;
    bottom: 28px;
    right: 28px;
    background: #25D366;
    color: white;
    text-decoration: none;
    padding: 14px 22px;
    border-radius: 50px;
    font-size: 14px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 8px;
    box-shadow: 0 8px 24px rgba(37,211,102,0.35);
    z-index: 999;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .whatsapp:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 32px rgba(37,211,102,0.5);
  }

  /* ── SCROLL REVEAL ── */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── MOBILE ── */
  @media (max-width: 900px) {
    header { padding: 16px 24px; }

    nav { display: none; flex-direction: column; position: absolute; top: 72px; left: 0; right: 0;
      background: var(--navy-mid); padding: 20px 24px; gap: 20px; border-bottom: 1px solid var(--border); }

    nav.open { display: flex; }

    .hamburger { display: flex; }

    .hero { padding: 100px 24px 60px; }

    .stats-band { padding: 50px 24px; }

    .section { padding: 70px 24px; }

    .modules-wrap, .contact-grid, .footer-top { grid-template-columns: 1fr; gap: 40px; }

    .form-row { grid-template-columns: 1fr; }

    footer { padding: 50px 24px 24px; }

    .footer-bottom { flex-direction: column; text-align: center; }

    .whatsapp .label { display: none; }
    .whatsapp { padding: 14px 16px; border-radius: 50%; }
  }
  
  header img{
height:60px;
}
</style>
</head>

<body>

<!-- HEADER -->
<header id="header">
  <div class="logo-area">
    <img src="logo.png">

  <!--   <div class="logo-icon">AF</div> -->
    <div class="logo-text">
      Advicore
      <span>Fin Technologies</span>
    </div>
  </div>

  <nav id="nav">
    <a href="#features">CBS Features</a>
    <a href="#modules">Modules</a>
    <a href="#pricing">Pricing</a>
    <a href="#testimonials">Clients</a>
    <a href="#contact" class="nav-cta">Contact Us</a>
  </nav>

  <div class="hamburger" id="hamburger" onclick="toggleNav()">
    <span></span><span></span><span></span>
  </div>
</header>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid"></div>

  <div class="hero-content">
    <div class="hero-badge">Next Generation Banking Technology</div>

    <h1>
      Core Banking<br>
      <span class="highlight">Built for Modern Finance</span>
    </h1>

    <p>Empowering Cooperative Banks, Microfinance Institutions &amp; Financial Organizations with robust, cloud-hosted CBS solutions.</p>

    <div class="hero-actions">
      <button class="btn-primary" onclick="document.getElementById('demo').scrollIntoView({behavior:'smooth'})">
        🚀 View Live Demo
      </button>
      <button class="btn-outline" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">
        Request a Quote
      </button>
    </div>

    <div class="hero-float">
      <div class="float-card">
        <div class="icon">🏦</div>
        <div>
          <div style="font-weight:600;font-size:13px;">Cooperative Banks</div>
          <div style="color:#94a3b8;font-size:12px;">Full CBS Support</div>
        </div>
      </div>
      <div class="float-card">
        <div class="icon">💳</div>
        <div>
          <div style="font-weight:600;font-size:13px;">MFI / SHG / JLG</div>
          <div style="color:#94a3b8;font-size:12px;">Microfinance Ready</div>
        </div>
      </div>
      <div class="float-card">
        <div class="icon">☁️</div>
        <div>
          <div style="font-weight:600;font-size:13px;">Cloud Hosted</div>
          <div style="color:#94a3b8;font-size:12px;">Secure &amp; Scalable</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- STATS -->
<div class="stats-band reveal">
  <div class="stat-item"><div class="num">50+</div><p>Banking Features</p></div>
  <div class="stat-item"><div class="num">10+</div><p>Core Modules</p></div>
  <div class="stat-item"><div class="num">100%</div><p>Secure Platform</p></div>
  <div class="stat-item"><div class="num">24/7</div><p>Support Available</p></div>
</div>

<!-- FEATURES -->
<section id="features" class="section">
  <div class="features-header reveal">
    <span class="section-label">What We Offer</span>
    <h2 class="section-title">Comprehensive Core Banking Features</h2>
    <p class="section-subtitle">A complete banking platform designed from the ground up for cooperative banks and microfinance institutions.</p>
  </div>

  <div class="features-grid">
    <div class="feature-card reveal">
      <span class="feature-emoji">🤝</span>
      <h3>Microfinance Support</h3>
      <p>Designed for MFIs with full SHG and JLG loan management, field officer dashboards and group meeting tracking.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">📅</span>
      <h3>Flexible Loan Schedules</h3>
      <p>Supports Daily, Weekly and Monthly repayment plans with both Flat and Reducing Balance interest calculation methods.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">💰</span>
      <h3>Diverse Loan Products</h3>
      <p>EMI Loans, SHG/JLG Loans, Cash Credit (CC), Overdraft (OD), Gold Loans and Loan Against Deposits — all in one platform.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">🏧</span>
      <h3>Deposit Modules</h3>
      <p>Savings Bank, Current Account, Fixed Deposit, Recurring Deposit, Daily Deposit, Thrift and Additional Thrift modules.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">👥</span>
      <h3>Membership & Shares</h3>
      <p>Member registration, share capital management and full cooperative society membership lifecycle tracking.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">🏢</span>
      <h3>Multi-Branch Banking</h3>
      <p>Head Office module with inter-branch transactions, centralized MIS reporting and consolidated balance sheets.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">☁️</span>
      <h3>Cloud Hosted</h3>
      <p>Secure cloud deployment with role-based access control, enterprise-grade security and zero infrastructure cost for the bank.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">⚙️</span>
      <h3>Branch Operations</h3>
      <p>Branch Pre-Close, Day Begin and Day End processes with automated validation checks and audit trails.</p>
    </div>
    <div class="feature-card reveal">
      <span class="feature-emoji">📊</span>
      <h3>Accounting & Reports</h3>
      <p>Daybook, Trial Balance, Balance Sheet, Profit &amp; Loss, and all statutory accounting reports required by regulators.</p>
    </div>
  </div>
</section>

<!-- MODULES -->
<section id="modules" class="section" style="background:var(--navy-mid)">
  <div class="modules-wrap">
    <div class="reveal">
      <span class="section-label">Platform Modules</span>
      <h2 class="section-title">Everything in One Platform</h2>
      <p class="section-subtitle" style="margin-bottom:16px">Our CBS is organized into powerful modules that work together seamlessly.</p>
      <div class="modules-list">
        <div class="module-item"><div class="module-dot"></div><span>Loan Management System</span></div>
        <div class="module-item"><div class="module-dot"></div><span>Deposit Management System</span></div>
        <div class="module-item"><div class="module-dot"></div><span>Membership & Share Module</span></div>
        <div class="module-item"><div class="module-dot"></div><span>Accounting & General Ledger</span></div>
        <div class="module-item"><div class="module-dot"></div><span>MIS & Reporting Engine</span></div>
        <div class="module-item"><div class="module-dot"></div><span>Head Office & Branch Module</span></div>
        <div class="module-item"><div class="module-dot"></div><span>User Administration & Audit</span></div>
      </div>
    </div>

    <div class="modules-visual reveal">
      <div style="font-size:48px;margin-bottom:12px;">🏛️</div>
      <h3 style="font-family:'Sora',sans-serif;font-size:20px;font-weight:700;margin-bottom:8px;">Supported Institution Types</h3>
      <p style="color:var(--text-muted);font-size:14px;margin-bottom:20px;">Our CBS is trusted by a wide range of financial institutions</p>
      <div class="module-tags">
        <span class="tag">Cooperative Banks</span>
      <!--  <span class="tag">Urban Co-op Banks</span> -->
        <span class="tag">Microfinance (MFI)</span>
        <span class="tag">SHG Groups</span>
        <span class="tag">JLG Groups</span>
        <span class="tag">Credit Societies</span>
      <!--  <span class="tag">NBFC-MFI</span> -->
        <span class="tag">Thrift Societies</span>
      </div>
    </div>
  </div>
</section>

<!-- DEMO CTA -->
<section id="demo" class="demo-section">
  <div style="position:relative;z-index:1">
    <span class="section-label">Live Platform</span>
    <h2>See It In Action</h2>
    <p>Experience a full walkthrough of our Core Banking System — no signup required.</p>
    <button class="btn-primary" onclick="window.open('https://apex.oracle.com','_blank')">
      🖥️ Launch CBS Demo
    </button>
  </div>
</section>

<!-- PRICING -->
<section id="pricing" class="section" style="background:var(--navy-mid)">
  <div class="reveal" style="text-align:center;margin-bottom:60px">
    <span class="section-label">Plans & Pricing</span>
    <h2 class="section-title">Transparent, Flexible Pricing</h2>
    <p class="section-subtitle" style="margin:0 auto">Pricing is tailored to your institution size and modules needed. Get in touch for a custom quote.</p>
  </div>

  <div class="pricing-grid">
    <div class="price-card reveal">
      <h3>Basic CBS</h3>
      <p style="color:var(--text-muted);font-size:14px;margin-top:8px">Single branch, essential modules</p>
      <div class="price-amount">Contact Us</div>
      <div class="price-sub">Custom quote based on membership size</div>
      <ul class="price-features">
        <li>Loan & Deposit Management</li>
        <li>Membership & Share Module</li>
        <li>Basic Accounting & Reports</li>
        <li>Day Begin / Day End Process</li>
        <li>Cloud Hosting Included</li>
      </ul>
      <button class="price-btn price-btn-secondary" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Quote</button>
    </div>

    <div class="price-card featured reveal">
      <div class="price-badge">⭐ Most Popular</div>
      <h3>Professional</h3>
      <p style="color:var(--text-muted);font-size:14px;margin-top:8px">Multi-branch with full reporting</p>
      <div class="price-amount">Custom</div>
      <div class="price-sub">Tailored for your institution</div>
      <ul class="price-features">
        <li>Everything in Basic</li>
        <li>Multi-Branch Banking</li>
        <li>SHG / JLG Loan Management</li>
        <li>Gold Loan & OD / CC Products</li>
        <li>MIS Reports & Analytics</li>
        <li>Priority Support</li>
      </ul>
      <button class="price-btn price-btn-primary" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Quote</button>
    </div>

    <div class="price-card reveal">
      <h3>Enterprise</h3>
      <p style="color:var(--text-muted);font-size:14px;margin-top:8px">Complete digital banking platform</p>
      <div class="price-amount">Custom</div>
      <div class="price-sub">For large cooperative banks</div>
      <ul class="price-features">
        <li>Everything in Professional</li>
        <li>Head Office + All Branches</li>
        <li>Custom Reports & Integrations</li>
        <li>Dedicated Support Manager</li>
        <li>On-site Training Included</li>
        <li>Regulatory Compliance Reports</li>
      </ul>
      <button class="price-btn price-btn-secondary" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Quote</button>
    </div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section id="testimonials" class="section">
  <div class="reveal" style="text-align:center;margin-bottom:60px">
    <span class="section-label">Client Feedback</span>
    <h2 class="section-title">Trusted by Banking Institutions</h2>
    <p class="section-subtitle" style="margin:0 auto">What our clients say about Advicore's Core Banking platform.</p>
  </div>

  <div class="testimonials-grid">
    <div class="testi-card reveal">
      <div class="stars">★★★★★</div>
      <blockquote>"The SHG and JLG loan management module has completely transformed how we handle our microfinance portfolio. Excellent support team."</blockquote>
      <div class="testi-author">
        <div class="testi-avatar">RK</div>
        <div>
          <div class="testi-name">Jagan</div>
          <div class="testi-role">CEO, Spoorthi Co-operative society</div>
        </div>
      </div>
    </div>

    <div class="testi-card reveal">
      <div class="stars">★★★★★</div>
      <blockquote>"Multi-branch operations are now seamless. The inter-branch transaction module and centralised reporting saved us weeks of manual work every month."</blockquote>
      <div class="testi-author">
        <div class="testi-avatar">PS</div>
        <div>
          <div class="testi-name">Heamanth</div>
          <div class="testi-role">Manager, Shiv Jyothi MFI</div>
        </div>
      </div>
    </div>

    <div class="testi-card reveal">
      <div class="stars">★★★★★</div>
      <blockquote>"From Day Begin to Day End, every process is automated and audited. The cloud hosting means zero IT overhead for our small team."</blockquote>
      <div class="testi-author">
        <div class="testi-avatar">SN</div>
        <div>
          <div class="testi-name">Navodaya</div>
          <div class="testi-role">Senior Branch Manager, Navodaya Thrift Society</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact" class="section" style="background:var(--navy-mid)">
  <div class="contact-grid">
    <div class="contact-info reveal">
      <span class="section-label">Get In Touch</span>
      <h2>Let's Discuss Your<br>Banking Needs</h2>
      <p>Fill out the form and our team will get back to you within 24 hours with a personalised demo and quote for your institution.</p>

      <div class="contact-detail">
        <div class="icon">📞</div>
        <div>
          <strong>+91 96529 88975</strong>
          <p>Mon–Sat, 9 AM – 6 PM IST</p>
        </div>
      </div>

      <div class="contact-detail">
        <div class="icon">💬</div>
        <div>
          <strong>WhatsApp Support</strong>
          <p>Quick responses on WhatsApp</p>
        </div>
      </div>

      <div class="contact-detail">
        <div class="icon">🌐</div>
        <div>
          <strong>Advicore Fin Technologies</strong>
          <p>Serving cooperative banks across India</p>
        </div>
      </div>
    </div>

    <div class="contact-form reveal">
      <h3>Send an Enquiry</h3>
      <form action="https://formspree.io/f/xwvrpray" method="POST">
        <div class="form-row">
          <div class="form-group">
            <label>Your Name *</label>
            <input type="text" name="name" placeholder="Full name" required>
          </div>
          <div class="form-group">
            <label>Phone Number *</label>
            <input type="tel" name="phone" placeholder="+91 XXXXX XXXXX" required>
          </div>
        </div>
        <div class="form-group">
          <label>Email Address</label>
          <input type="email" name="email" placeholder="you@organisation.com">
        </div>
        <div class="form-group">
          <label>Bank / Organisation *</label>
          <input type="text" name="organization" placeholder="Name of your institution" required>
        </div>
        <div class="form-group">
          <label>Type of Institution</label>
          <select name="type">
            <option value="">Select institution type</option>
            <option>Cooperative Bank</option>
            <option>Urban Co-operative Bank</option>
            <option>Microfinance Institution (MFI)</option>
            <option>NBFC-MFI</option>
            <option>Credit Society</option>
            <option>Thrift Society</option>
            <option>Other</option>
          </select>
        </div>
        <div class="form-group">
          <label>Message</label>
          <textarea name="message" rows="4" placeholder="Tell us about your requirements..."></textarea>
        </div>
        <button type="submit" class="form-submit">Send Enquiry →</button>
      </form>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-top">
    <div class="footer-brand">
      <div class="logo-area" style="margin-bottom:14px">
        <div class="logo-icon">AF</div>
        <div class="logo-text">Advicore<span>Fin Technologies</span></div>
      </div>
      <p>Next-generation Core Banking platform built for cooperative banks, microfinance institutions and financial organisations across India.</p>
    </div>


    <div class="footer-col">
      <h4>Product</h4>
      <a href="#features">CBS Features</a>
      <a href="#modules">Platform Modules</a>
      <a href="#pricing">Pricing</a>
      <a href="#demo">Live Demo</a>
    </div>

    <div class="footer-col">
      <h4>Solutions</h4>
      <a href="#">Cooperative Banks</a>
      <a href="#">Microfinance (MFI)</a>
      <a href="#">SHG / JLG Groups</a>
      <a href="#">Credit Societies</a>
    </div>

    <div class="footer-col">
      <h4>Company</h4>
      <a href="#contact">Contact Us</a>
      <a href="https://wa.me/919652988975" target="_blank">WhatsApp</a>
      <a href="#">Privacy Policy</a>
      <a href="#">Terms of Service</a>
    </div>
  </div>

  <div class="footer-bottom">
    <p>© 2026 Advicore Fin Technologies. All Rights Reserved.</p>
    <div class="footer-links">
      <a href="#">Privacy Policy</a>
      <a href="#">Terms</a>
      <a href="#contact">Support</a>
    </div>
  </div>
</footer>

<!-- WHATSAPP FAB -->
<a class="whatsapp" href="https://wa.me/919652988975" target="_blank">
  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
    <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"/>
    <path d="M12 0C5.373 0 0 5.373 0 12c0 2.127.558 4.122 1.532 5.855L0 24l6.334-1.511A11.945 11.945 0 0012 24c6.627 0 12-5.373 12-12S18.627 0 12 0zm0 21.818a9.818 9.818 0 01-5.012-1.372l-.36-.214-3.733.891.907-3.625-.235-.374A9.818 9.818 0 1112 21.818z"/>
  </svg>
  <span class="label">Chat on WhatsApp</span>
</a>

<script>
  // Mobile nav toggle
  function toggleNav() {
    document.getElementById('nav').classList.toggle('open');
  }

  // Close nav on link click (mobile)
  document.querySelectorAll('nav a').forEach(a => {
    a.addEventListener('click', () => {
      document.getElementById('nav').classList.remove('open');
    });
  });

  // Scroll reveal
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // Animated counter
  function animateCount(el, target, suffix='') {
    let start = 0;
    const dur = 1800;
    const step = (timestamp) => {
      if (!start) start = timestamp;
      const progress = Math.min((timestamp - start) / dur, 1);
      const val = Math.floor(progress * target);
      el.textContent = val + suffix;
      if (progress < 1) requestAnimationFrame(step);
      else el.textContent = target + suffix;
    };
    requestAnimationFrame(step);
  }

  const statsObs = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        animateCount(document.querySelector('.stats-band .stat-item:nth-child(1) .num'), 50, '+');
        animateCount(document.querySelector('.stats-band .stat-item:nth-child(2) .num'), 10, '+');
        statsObs.disconnect();
      }
    });
  }, { threshold: 0.3 });

  const statsBand = document.querySelector('.stats-band');
  if (statsBand) statsObs.observe(statsBand);

  // Active nav link on scroll
  const sections = document.querySelectorAll('section[id]');
  window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(s => {
      if (window.scrollY >= s.offsetTop - 120) current = s.id;
    });
    document.querySelectorAll('nav a').forEach(a => {
      a.style.color = a.getAttribute('href') === '#'+current ? 'var(--white)' : '';
    });
  });
</script>

</body>
</html>
